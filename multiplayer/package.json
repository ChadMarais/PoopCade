{
  "name": "poopcade-arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787 --ip 127.0.0.1",
    "dev:lan": "wrangler dev --port 8787 --ip 0.0.0.0",
    "check": "wrangler deploy --dry-run --outdir .wrangler/check",
    "test": "node --experimental-strip-types --test test/*.test.ts test/*.test.mjs",
    "test:wire": "node --experimental-strip-types test/local-ws-harness.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
